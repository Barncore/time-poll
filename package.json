{
  "name": "time-poll",
  "version": "1.0.0",
  "description": "Group mentorship session time poll",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "npx nodemon server.js",
    "pages:dev": "wrangler pages dev public/",
    "deploy": "wrangler pages deploy public/ --project-name time-poll"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
